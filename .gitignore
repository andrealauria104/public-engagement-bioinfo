# OS
.DS_Store

# Claude Code local state (machine-specific, not shared)
.claude/settings.local.json
**/.claude/.cc-writes/

# Node
node_modules/
dist/

# Raw/intermediate GTEx data (large, regeneratable / public download) --
# only gtex-data/src/ (the R scripts) is version-controlled.
gtex-data/data/
gtex-data/results/
