name: Deploy to GitHub Pages

# Each project is served under /<folder>/; site/ holds the root landing page.
on:
  push:
    branches: [main]
    paths:
      - 'gtex-game/**'
      - 'site/**'
      - '.github/workflows/deploy-pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: gtex-game/package-lock.json

      - name: Install dependencies
        working-directory: gtex-game
        run: npm ci

      - name: Build
        working-directory: gtex-game
        run: npm run build

      - name: Assemble site
        run: |
          mkdir -p _site
          cp -r site/. _site/
          cp -r gtex-game/dist _site/gtex-game

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
